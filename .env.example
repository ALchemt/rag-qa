# Set at least one LLM provider. Priority: GROQ > OPENROUTER > HF.
GROQ_API_KEY=
OPENROUTER_API_KEY=
HF_TOKEN=

# Optional: override auto-picked default model
LLM_MODEL=

CHROMA_PATH=./chroma_db
CORPUS_PATH=./docs
EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2
TOP_K=5
MMR_LAMBDA=0.5
